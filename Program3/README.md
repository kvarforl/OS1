Code in this directory is a C implementation of a small shell. The shell supports comments (start with #), file redirection (< , >), and background processes (end with in &).
It also demonstrates signal handling.

Usage:
```
make
./smallsh
```
